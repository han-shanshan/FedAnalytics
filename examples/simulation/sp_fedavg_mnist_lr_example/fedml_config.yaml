common_args:
  training_type: "simulation"
  random_seed: 0

data_args:
  dataset: "fake"
  data_cache_dir: /data

train_args:
  federated_optimizer: "FedAvg"
  client_num_in_total: 4
  client_num_per_round: 4
  comm_round: 10
  task: "AVG"

comm_args:
  backend: "sp"

